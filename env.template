# AWS Configuration
# ==================

# Required: AWS Region for Bedrock and Transcribe
AWS_REGION=us-west-2

# Required: Enable Bedrock for Claude Code
CLAUDE_CODE_USE_BEDROCK=1

# Optional: AWS Credentials (if not using aws configure or SSO)
# Uncomment and fill in if needed:
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
# AWS_SESSION_TOKEN=your-session-token

# Optional: For AWS SSO
# AWS_PROFILE=your-profile-name

# Transcription Configuration
# ============================

# Required: S3 bucket for temporary audio storage
# This bucket must exist in your AWS account
# Update this to match your bucket name (create with: aws s3 mb s3://your-bucket-name)
MEETING_TRANSCRIBE_S3_BUCKET=professional-growth-transcriptions

# Optional: Keep audio files in S3 after transcription (default: true = delete)
CLEANUP_AUDIO_FROM_S3=true

# Claude Model Configuration (Optional)
# ======================================

# Optional: Override default model
# ANTHROPIC_MODEL=global.anthropic.claude-sonnet-4-5-20250929-v1:0

# Optional: Override small/fast model
# ANTHROPIC_SMALL_FAST_MODEL=us.anthropic.claude-haiku-4-5-20251001-v1:0

# Optional: Disable prompt caching if needed
# DISABLE_PROMPT_CACHING=0

# Recommended: Token limits for Bedrock
CLAUDE_CODE_MAX_OUTPUT_TOKENS=4096
MAX_THINKING_TOKENS=1024

